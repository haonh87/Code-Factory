{
  "name": "@code-factory/session-search-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Read-only MCP server for searching local coding-agent session history through cass",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test",
    "check": "node --test && node --check src/core.js && node --check src/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "latest",
    "zod": "^3.25.0"
  }
}
