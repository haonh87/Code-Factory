{
  "name": "@code-factory/notebooklm-mcp-launcher",
  "version": "0.1.0",
  "private": true,
  "description": "Thin launcher for the upstream notebooklm-mcp server via uvx",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test",
    "check": "node --test && node --check src/core.js && node --check src/index.js"
  }
}
