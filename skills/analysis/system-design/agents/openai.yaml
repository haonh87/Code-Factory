interface:
  display_name: "System Design"
  short_description: "Chốt technical approach DEV-led với failure, compatibility, rollback và observability rõ"
  default_prompt: "Dùng $system-design để chốt technical approach, boundary, interface change, failure modes, compatibility, rollback và observability hooks trước khi sang task-breakdown-planner."

policy:
  allow_implicit_invocation: true
