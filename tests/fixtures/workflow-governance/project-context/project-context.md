# Fixture Project Context

Đây là `project-context` tối giản cho fixture suite governance validator.

- `governance_ref` mặc định trỏ file này.
- `default`, `strict`, `regulated`, `custom` dùng đúng enum chính thức của workflow.
- Exception hoặc waiver cần audit phải được ghi trong `governance-exception-register.md`.
